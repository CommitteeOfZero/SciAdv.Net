<UserControl x:Class="ProjectAmadeus.Views.GameStringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ProjectAmadeus.ViewModels"
             xmlns:local="clr-namespace:ProjectAmadeus.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=vm:GameStringViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <TextBox Text="{Binding DialogueLine}" />
    </Grid>
</UserControl>
