<UserControl x:Class="ProjectAmadeus.Views.Notifications.SaveChangesPromptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ProjectAmadeus.ViewModels.Notifications"
             mc:Ignorable="d" FontSize="18"
             d:DesignHeight="100" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=vm:SaveChangesPromptViewModel, IsDesignTimeCreatable=True}">
    <TextBlock TextWrapping="Wrap">
        Would you like to save changes to <Run Text="{Binding FileName}" />?
    </TextBlock>
</UserControl>
